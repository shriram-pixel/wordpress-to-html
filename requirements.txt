# wp-static-converter dependencies
#
# Floor-pinned rather than exact-pinned: the tool targets Python 3.11 through
# 3.14, and exact pins would force source builds on the newer interpreters
# where the older wheels do not exist. Run scripts/freeze.ps1 to produce an
# exact lock file for a reproducible deployment.

# ---- Web framework / job API -------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.34
python-multipart>=0.0.20
jinja2>=3.1.4
pydantic>=2.9
pydantic-settings>=2.6

# ---- Browser rendering -------------------------------------------------------
playwright>=1.49

# ---- HTML / CSS parsing & rewriting -----------------------------------------
beautifulsoup4>=4.12
soupsieve>=2.5
tinycss2>=1.3
# lxml parses pages about 1.7x faster than the stdlib parser, which is the
# single biggest saving in HTML generation and link checking. html_processor
# falls back to the stdlib parser automatically when it is absent.
lxml>=6.0

# ---- HTTP (asset fetching / validation) -------------------------------------
httpx>=0.27

# ---- Visual diffing ----------------------------------------------------------
pillow>=11.0
numpy>=2.1

# ---- Tests -------------------------------------------------------------------
pytest>=8.3
pytest-asyncio>=0.24

# ---- MySQL access (pure Python, no C driver needed) -------------------------
pymysql>=1.1
