[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    integration: needs PHP, MariaDB, Chromium and the demo .wpress fixture
    slow: takes more than a few seconds
filterwarnings =
    ignore::DeprecationWarning:playwright.*
